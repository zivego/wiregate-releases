Place your final product image here as:

- `web/app/public/branding/wiregate-logo.png`

The UI uses this PNG automatically for header/login icons.
If the PNG is missing, the app falls back to `wiregate-logo.svg`.
